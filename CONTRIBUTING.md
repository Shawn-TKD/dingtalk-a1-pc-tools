# Contributing

欢迎提交协议解析修正、跨平台兼容性改进和不破坏设备的只读功能。

提交前请确认：

1. 研究对象属于你或你有明确授权；
2. 没有提交真实密钥、账号标识、序列号、MAC、抓包、APK 或录音；
3. 新命令首先在文档中标明读写影响；
4. 不加入 reset、unbind、OTA、固件写入、密钥猜测或批量陌生设备操作；删除功能必须保持“当前索引单条、输入完整确认码、无备份使用 `NOBACKUP` 强确认”的安全门槛；
5. 运行 README 中的 Python 测试和前端构建。

协议结论请注明证据类型：实物抓包、离线重放、静态分析或尚未验证的推断。请避免把推断写成事实。
